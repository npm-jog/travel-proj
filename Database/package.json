{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "seed": "node ./db/seed/run-seed.js",
    "seed-prod": "NODE_ENV=production npm run seed",
    "reset-db": "NODE_ENV=production node ./db/resetDatabase.js && NODE_ENV=development node ./db/resetDatabase.js && NODE_ENV=test node ./db/resetDatabase.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "mongoose": "^8.0.0"
  }
}
