function fetchReviewsByLocation() {
	const myPromise: Promise<string> = new Promise((resolve, reject) => {
		resolve("hello" + "world");
	});
	return myPromise;
}

function addReviewToDb() {
	const myPromise: Promise<string> = new Promise((resolve, reject) => {
		resolve("hello" + "world");
	});
	return myPromise;
}

function removeReviewFromDb() {
	const myPromise: Promise<string> = new Promise((resolve, reject) => {
		resolve("hello" + "world");
	});
	return myPromise;
}

function editReviewInDb() {
	const myPromise: Promise<string> = new Promise((resolve, reject) => {
		resolve("hello" + "world");
	});
	return myPromise;
}

export {
	fetchReviewsByLocation,
	addReviewToDb,
	removeReviewFromDb,
	editReviewInDb,
};
